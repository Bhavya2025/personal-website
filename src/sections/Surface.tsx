import Birds from '../components/Birds'

/** Home landing — the daylight you descend into, with drifting birds. */
function Surface() {
  return (
    <section className="surface surface--landing" aria-hidden="true">
      <Birds />
    </section>
  )
}

export default Surface
